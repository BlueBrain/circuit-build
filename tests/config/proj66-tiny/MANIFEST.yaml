common:
  atlas: entities/atlas/O1-152
  region: 'mc2_Column'
  mask: '[mask]mc2'
  morph_release: entities/morphologies/
  emodel_release: entities/emodels/
  node_population_name: 'neocortex_neurons'
  edge_population_name: 'neocortex_neurons__chemical_synapse'

place_cells:
  soma_placement: 'basic'
  density_factor: 0.1
  sort_by: ['region', 'mtype']
  seed: 0
  mini_frequencies: True

choose_morphologies:
  alpha: 3.0
  seed: 0

assign_morphologies:
  max_drop_ratio: 0.1
  seed: 0

assign_emodels:
  seed: 0

node_sets:
  targets: 'targets.yaml'
